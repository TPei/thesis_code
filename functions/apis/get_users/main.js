// endpoint called when querying for all users
function main (params) {
  // TODO: prepare stuff to return data from all users
  // - get all yappl policies
  // - parse yappl (?)
  // - get all user data
  // - apply yappl transformations
  return { data: '' }
}
